Quick and dirty solution to send extemporary notifications.
You can add a custom field ('Cc' by default) and specify with it a mix of email addresses and users that have to be notified of the current update.
